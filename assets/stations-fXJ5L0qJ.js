const e=[{key:"cpu-cores-cache",tab:"CPU",title:"CPU — cores & cache",accent:"cpu",fault:"cpu",one_liner:"Runs work on 16 real cores; the cache hierarchy exists to hide that DRAM is ~85× slower than L1.",mechanism:"A core retires instructions in well under a nanosecond, but a DRAM fetch costs ~120ns (random+TLB) — the memory wall. L1/L2 are per-core, L3 is shared per socket; memory moves in 64-byte lines, so performance is locality — keeping a thread’s working set in cache. MESI keeps lines coherent: a write invalidates every other copy, so two cores updating different vars in one 64-byte line ping-pong it (false sharing). Saturation isn’t high utilization — it’s runnable tasks (run-queue) exceeding the 16 cores; with no SMT here that’s a hard 16-way ceiling, and the average across cores hides a single pinned core.",readouts:[{label:"run-queue",field:"cpu.val",unit:"tasks",watch:"> 16 (nCPU) = saturation, tasks queue for a core"},{label:"core util avg",field:"cores.avg",unit:"%",watch:"high only matters with run-queue > nCPU; util alone isn’t saturation"},{label:"hottest core",field:"cores.max",unit:"%",watch:"~100% while avg low = a single-core bottleneck the average hides"},{label:"hot-core index",field:"cores.hot",unit:"core#",watch:"one core pinned (often %soft on one RX queue) while others idle"},{label:"core count",field:"nq.ncpu",unit:"cores",watch:"the ceiling — 16, no SMT; run-queue is judged against this"},{label:"cgroup throttle",field:"cg.thr_pct",unit:"%",watch:"> 0 = cpu.max quota exhausted; shows as nr_throttled, never as steal"}],costs:[["L1 hit","1.4 ns"],["L2 hit","~4 ns"],["DRAM (random+TLB)","~120 ns"],["mem bandwidth","~28 GB/s/core"],["context switch","~1.7 µs"],["cache line","64 B"]],fails:[{sig:"Genuinely CPU-bound (the only true case)",bad:"run-queue ≈ 16 sustained AND avg high — cores really compute your code; profile the hot path"},{sig:"Overloaded scheduler",bad:"run-queue ≫ 16 (20 spinners drove it to 21) + involuntary ctx-switches spiking ⇒ thrash; cut concurrency to ≈16"},{sig:"Per-core skew — the average lies",bad:"hottest ~100% while avg ≈ 5% ⇒ one core is the bottleneck; spread work (RSS/RPS), adding cores won’t help"},{sig:"Working set spills L3 → DRAM",bad:"LLC-load-misses >~20–30% (perf stat) or false-sharing HITM>0 (perf c2c) ⇒ every miss pays ~120ns; p99 grows"}],ref:{ids:["cpu-cache-numa","perf-cpu","eevdf","perf-method"],note:"the memory wall + MESI/false-sharing + NUMA, the run-queue-vs-nCPU saturation triage, who-gets-the-core, and CPU as a coupled subsystem."}},{key:"scheduler",tab:"Scheduler",title:"Scheduler — the run queue",accent:"cpu",fault:"cpu",one_liner:"Decides which runnable thread gets a core next — fairness from vruntime (this box is kernel 6.1 = CFS); nice sets the weight, not a dispatch priority.",mechanism:"Each core keeps its own run queue (cfs_rq): an rbtree of runnable sched_entities keyed by vruntime (virtual runtime). This box runs kernel 6.1, so the fair class is CFS — it dispatches the leftmost node, the smallest vruntime, i.e. the task that has had the least weighted CPU. vruntime advances ∝ 1/weight, so a heavier (lower-nice) task accumulates it slower and earns more CPU — nice sets the share, it is NOT a dispatch priority (only the RT classes dispatch by priority, and ~nothing here is RT). Preemption fires on the tick or when a waking task has a smaller vruntime; a load balancer migrates tasks off busy cores, favoring cache/NUMA affinity. (6.6+ replaced CFS with EEVDF, which adds an eligibility test + a per-task virtual deadline as a latency knob — not this kernel.) A cgroup cpu.max quota layers on top: burn the quota and the guest kernel parks the group runnable-but-not-dispatched until the next CFS period — the box looks pressured while silicon idles (this is the guest scheduler, never %steal).",readouts:[{label:"run-queue depth",field:"cpu.val",unit:"runnable",watch:"> nCPU (16); ≫16 = tasks queueing (20 spinners → 21)"},{label:"PSI-cpu some",field:"psi.cpu",unit:"",watch:"real Linux PSI (some, avg10, 0..100): > 0 = tasks waited for a core; sustained tens = saturation"},{label:"run-queue latency",field:"ebpf.sched.rqlat_us",unit:"µs",watch:"> ~100µs sustained = tasks waiting for a core — the real saturation latency, not just depth (eBPF runqlat)"},{label:"ctx switches/s",field:"ebpf.sched.cs",unit:"/s",watch:"climbs with run-queue ≫ nCPU ⇒ thrash; involuntary switches shred time-slices (eBPF sched_switch)"},{label:"core util avg",field:"cores.avg",unit:"%",watch:"high WITH run-queue ≤ 16 is healthy; only a problem once run-queue exceeds cores"},{label:"hottest core",field:"cores.max",unit:"%",watch:"~100 while avg low ⇒ per-core skew (one RX queue / pinned thread)"},{label:"cgroup throttled",field:"cg.thr_pct",unit:"%",watch:"> ~10 sustained ⇒ cpu.max parking the group; threads runnable, host idle"},{label:"throttled periods",field:"cg.nr_throttled",unit:"count",watch:"climbing ⇒ recurring quota stalls — alert on this, not %steal"}],costs:[["context switch","~1.7 µs"],["pipe round-trip","~3.4 µs"],["syscall trap (svc)","~83 ns"],["saturation point","run-queue > 16"],["validated","20 spinners → cpu.val 21, cpu PSI climbs"]],fails:[{sig:"Overloaded — too many runnable threads",bad:"run-queue ≫ 16 + involuntary ctx-switch spiking ⇒ slices shredded, switch overhead eats real work; cut concurrency to ≈nCPU"},{sig:"cgroup throttle masquerading as idle",bad:"throttled % > 10 and periods climbing while avg low and PSI > 0 ⇒ cpu.max parks the group; prefer cpu.weight"},{sig:"Latency thread losing the deadline race",bad:"PSI > 0 with avg only moderate ⇒ a full-slice batch wins earliest-deadline; raise its cpu.weight / shrink slice"},{sig:"Per-core imbalance the aggregate hides",bad:"hottest ~100 while avg low ⇒ single pinned core; spread work (RSS/RPS), don’t add cores"}],ref:{ids:["eevdf","perf-cpu","cgroups","sched-ext","perf-method"],note:"the CFS vruntime pick on this 6.1 box (EEVDF is the 6.6+ successor), the run-queue-vs-nCPU triage, cpu.max throttling (why it isn’t %steal), the coupling view, and BPF-pluggable policy."}},{key:"mem",tab:"Memory",title:"Memory — virtual memory & reclaim",accent:"mem",fault:"mem",one_liner:"Hands every process its own address space, fills idle RAM with reclaimable cache, and does work — scan, evict, swap — only when free dips under a watermark.",mechanism:"Each process sees virtual addresses the MMU walks through 4-level page tables (cached in the TLB) into physical frames; a touch of an unmapped page faults — minor (~703ns: demand-zero, COW, already-cached) or major (disk, ~1000×). Resident pages split into file-backed (page cache — reclaim is free, the copy is on disk) and anonymous (heap/stack — reclaimable only by writing to swap). Per-zone watermarks gate reclaim: free under low wakes kswapd to scan the active/inactive LRU lists in the background; free hitting min forces the allocating thread into direct reclaim — that stall IS the latency spike. Here swap=0, so anon is unreclaimable: under pressure the kernel can only drop cache, and the floor is the OOM killer, not a swap death-spiral.",readouts:[{label:"MemAvailable",field:"vm.avail_gb",unit:"GB",watch:"< ~9 GB (~15% of 61.7); toward 0 = real pressure"},{label:"swap in/out",field:"mem.val",unit:"pg/s",watch:"any sustained > 0 (swap=0 here, so stays 0 — pressure goes straight to OOM)"},{label:"major faults",field:"vm.reclaim.majflt_s",unit:"/s",watch:"> 0 in steady state = refaulting from disk, working set doesn’t fit"},{label:"direct reclaim",field:"vm.reclaim.scan_direct_s",unit:"pg/s",watch:"> 0 = kswapd outrun, app threads stalling in their own reclaim"},{label:"free vs wmark",field:"vm.wmark_mb.free",unit:"MB",watch:"< low = kswapd active; < min = direct reclaim / allocation stall"},{label:"mem PSI",field:"psi.mem",unit:"",watch:"real PSI (some avg10): 0 at rest here, climbs under reclaim/refault; sustained > 0 = stalling on memory"}],costs:[["minor fault","~703 ns"],["major fault (EBS)","~544 µs (~770× minor)"],["DRAM vs L1","~120 ns vs 1.4 ns"],["mem bandwidth","~28 GB/s/core"],["swap headroom","0 GB — OOM is the floor"]],fails:[{sig:"Full but fine — reacting to free ≈ 0",bad:"MemAvailable healthy + PSI ≈ 0 while RAM looks 95% used — it’s reclaimable cache; fix the alert, not the box"},{sig:"Working set outgrew RAM (refault churn)",bad:"major faults > 0 sustained AND MemAvailable → 0 — every miss is a ~544µs disk round-trip; cap heap / right-size RAM"},{sig:"kswapd outrun → direct-reclaim stall",bad:"direct-reclaim > 0 while free < min — allocating threads block doing reclaim, p99 spikes"},{sig:"OOM kill (swap=0 floor)",bad:"swap=0, MemAvailable → 0, anon can’t be reclaimed, cg mem → 100% → kernel kills a process. No thrash precedes it — the drop is abrupt"}],ref:{ids:["vm-paging","page-cache","perf-vm","mmap-cow","memory-allocators"],note:"the MMU walk + fault classes + replacement (LRU/MGLRU), anon-vs-file + MemAvailable, the failure-first triage (free→PSI→si/so→majflt→pgscan), COW, and allocators."}},{key:"disk",tab:"Disk",title:"Disk — VFS · page cache · block",accent:"disk",fault:"disk",one_liner:"Make bytes durable and fast — serve most reads from RAM, queue the rest to a device that is really the network.",mechanism:"A read falls VFS → filesystem → page cache (a per-file xarray mapping offset → folio); a hit returns from RAM and never touches the device. A miss, or a buffered write, queues in the block layer (blk-mq: per-core software queues feeding the device’s hardware queues), where await = queue-wait + service-time and aqu-sz is the saturation signal. Buffered writes land dirty and are flushed asynchronously by writeback kworkers; fsync forces it now. The NVMe driver shares submission/completion queues in RAM, rings a doorbell, and the device DMAs the data — but here the device is EBS over the Nitro card, so one I/O is a ~0.5ms network round-trip and %util is meaningless on a parallel device.",readouts:[{label:"read await",field:"io.val",unit:"ms",watch:"> ~5ms sustained vs the ~0.5ms EBS floor"},{label:"dirty pages",field:"vm.dirty_mb",unit:"MB",watch:"climbing toward dirty_ratio ⇒ writers conscripted into sync flush (the freeze)"},{label:"page cache",field:"vm.cached_gb",unit:"GB",watch:"collapsing while majflt rises ⇒ working set evicted, reads now miss to disk"},{label:"bio latency (eBPF)",field:"ebpf.bio_lat_us",unit:"µs",watch:"> ~800µs (p99 EBS baseline 764µs) or bimodal"},{label:"writes",field:"disk.wps",unit:"w/s",watch:"high w/s + low KB ⇒ small-random, IOPS-bound; near rated MB/s ⇒ bandwidth-bound"},{label:"major faults",field:"vm.reclaim.majflt_s",unit:"/s",watch:"> 0 sustained ⇒ refault is disk I/O (swap=0 → cache refault)"}],costs:[["EBS 4K read await","~544 µs (p99 764)"],["page cache hit","RAM speed — ~120 ns"],["minor page fault","~703 ns"],["syscall trap","~83 ns"],["device floor","~0.5 ms net RTT (not local flash)"]],fails:[{sig:"%util lies on NVMe/EBS",bad:"await sub-ms + low IOPS but a dashboard %util ~100 — parallel device; trust await + aqu-sz, never order a faster disk"},{sig:"Writeback freeze under write load",bad:"dirty pinned near dirty_ratio while w/s spikes ⇒ multi-second sync-flush stalls; lower dirty_background_ratio"},{sig:"When VM kills I/O (the coupling)",bad:"await climbing with MemAvailable low / majflt > 0 ⇒ refault+writeback steal the device; fix RAM, not the disk"},{sig:"Hot working set evicted by a batch",bad:"page cache drops then majflt rises after a scan job ⇒ read tail; protect with memory.low or O_DIRECT for the batch"}],ref:{ids:["page-cache","perf-io","nvme","io-uring","disk-scheduling","inodes-vfs"],note:"RAM↔disk + the writeback freeze, the await/aqu-sz signals + the %util trap, the device floor + write cliff, blk-mq depth, and VFS/inode/journaling."}},{key:"net",tab:"Network",title:"Network — NIC · TCP/IP · sockets",accent:"net",fault:"net",one_liner:"Move bytes between sockets and the wire — and spread the receive work so one core isn’t the whole stack.",mechanism:"RX never rides one interrupt to the top: the ENA NIC DMAs a frame into an RX ring and raises one IRQ; the top-half schedules NET_RX_SOFTIRQ and masks further IRQs, then NAPI polls the ring in batches (bounded by netdev_budget), building an sk_buff per packet up through IP (routing/netfilter) and TCP (checksum, sequencing, socket lookup) into the recv queue, where read()/epoll wakes — TX mirrors it. RSS hashes flows onto multiple HW queues so different cores drain different traffic; a single active queue lands all softirq on one core, which IS the whole stack and caps far below line rate. Sockets are files: each is an fd indexing an open file description, so a leaked fd keeps a connection alive and the open-file limit ceilings all sockets at once. “Network is slow” is five diseases — window-vs-pipe, wire-error-vs-host-drop, loss-vs-bufferbloat, accept/conntrack queue, RX-softirq coupling — and almost none is bandwidth.",readouts:[{label:"RX softirq",field:"net.val",unit:"%soft",watch:"one core’s %soft → ~100 while others idle (one RX queue = whole stack)"},{label:"softirq pressure",field:"net.p",unit:"",watch:"→1.0 = one core saturated in softirq (busiest-core %soft/100; total softirq, usually but not only NET_RX). Not a PSI class — Linux has no net PSI"},{label:"RX queues spread",field:"nq.rxq_cores",unit:"cores",watch:"= 1 (or ≪ nCPU) under load ⇒ RSS not spreading, single-core ceiling"},{label:"retransmits",field:"netio.rtx_pct",unit:"%segs",watch:"> baseline (~0.1–1%) ⇒ loss collapsing cwnd, throughput sawtooths"},{label:"RX drops (host)",field:"netio.rx_drop_s",unit:"/s",watch:"> 0 ⇒ HOST overran a ring/qdisc — too few queues or starved softirq, not the wire"},{label:"TCP listen drops",field:"netio.tcp_drop_s",unit:"/s",watch:"> 0 ⇒ accept queue / backlog / conntrack overflow — plumbing, not the data path"}],costs:[["syscall trap (send/recv)","~83 ns"],["RTT loopback","~26 µs"],["RTT in-region","~1.5 ms"],["DRAM (sk_buff)","~120 ns"],["copy ceiling","~28 GB/s/core"]],fails:[{sig:"One core IS the network stack",bad:"one core %soft ≈ 100 with others idle AND RX queues = 1 — caps below line rate; fix = ethtool -L combined N + RSS, not more bandwidth"},{sig:"Host drops, not wire errors",bad:"rx_drop_s > 0 (ring/qdisc overrun) while ethtool rx_errors ≈ 0 — capacity/scheduling: add queues, bigger rings, raise netdev_budget"},{sig:"Retransmit storm collapsing cwnd",bad:"rtx % above path baseline (cross-check ebpf.tcp.rtx_s) ⇒ loss halving cwnd each round; fix loss / BBR, not the pipe"},{sig:"Connection-queue / conntrack exhaustion",bad:"tcp_drop_s > 0 or opens/s spiking with ListenOverflows climbing ⇒ raise somaxconn + listen backlog / nf_conntrack_max"}],ref:{ids:["net-rxtx","interrupts-dma","nic-offloads","perf-net","udp-vs-tcp","tcp-congestion","tcp-handshake","file-descriptors"],note:"the RX/TX path (DMA→NAPI→softirq→socket), NAPI/RSS/the one-core ceiling, the five-disease triage, and sockets-are-files."}},{key:"syscalls-interrupts",tab:"Syscalls",title:"Syscalls & interrupts",accent:"live",one_liner:"The only doors into the kernel: the syscall trap going down, the IRQ → softirq coming back up.",mechanism:"Userspace can’t touch hardware, so it traps in: on this aarch64 box the svc instruction switches to ring 0 and jumps to the entry handler, which dispatches via the syscall table (~83ns/trap); a few read-only calls (clock_gettime, getcpu) skip the trap by reading a kernel-mapped vDSO page in userspace. Coming back up, a device raises an MSI-X interrupt whose top half is tiny — it acknowledges and schedules deferred work, because the hard-IRQ path runs with interrupts disabled. The bottom half (softirq / NAPI poll) does the real processing with IRQs re-enabled, and the data never rides the CPU — the device DMA’d it straight to/from RAM along descriptor rings, IOMMU-fenced. Which core services each device’s IRQ is a lever (RSS/affinity): spread, the load balances; pinned, that one core becomes the entire receive path.",readouts:[{label:"softirq (busiest core)",field:"net.val",unit:"%soft",watch:"one core ~100 while avg low ⇒ single-RX-queue ceiling, one core IS the stack"},{label:"hottest core",field:"cores.max",unit:"%",watch:"~100 while avg < 30 ⇒ per-core skew; the average hides a melting core"},{label:"RX-queue spread",field:"nq.rxq_cores",unit:"cores",watch:"< nCPU (esp. = 1) ⇒ receive serializes; IRQs not spread by RSS/affinity"},{label:"top IRQ rate",field:"irq.net",unit:"irq/s",watch:"approaching pps (one IRQ per packet) ⇒ coalescing off / NAPI not holding poll"},{label:"syscalls/s (eBPF)",field:"ebpf.sys_s",unit:"/s",watch:"very high + %sy climbing ⇒ crossing-bound hot loop; batch with io_uring"},{label:"net syscalls/s (eBPF)",field:"ebpf.sys_net_s",unit:"/s",watch:"dominates total on a hot RPC loop ⇒ one tiny recv/send per op, no batching"}],costs:[["syscall trap (svc)","~83 ns"],["context switch","~1.7 µs"],["minor page fault","~703 ns"],["vDSO clock_gettime","~0 ns (no trap)"],["loopback RTT","~26 µs"]],fails:[{sig:"One core’s softirq pinned",bad:"busiest %soft ~100 while avg low and RX queues = 1 — every device IRQ lands on one core, its ksoftirqd serializes the whole NIC path"},{sig:"Syscall-bound hot loop (mode-switch tax)",bad:"%sy high + climbing with syscalls/s very high — each ~83ns trap + copy eats CPU. strace -c to confirm; fix = batch (io_uring)"},{sig:"IRQ storm — one interrupt per packet",bad:"IRQ rate tracks pps ~1:1 (coalescing off / NAPI fell out of poll) — CPU pays full interrupt entry per frame"},{sig:"CPU starvation drops packets",bad:"avg saturated → ksoftirqd can’t drain the RX ring in netdev_budget → rx_drop_s climbs and surfaces as retransmits (Network↔CPU coupling)"}],ref:{ids:["syscalls","interrupts-dma","perf-cpu","perf-net","net-rxtx","io-uring"],note:"the trap + vDSO fast path, top/bottom half + DMA + NAPI + IRQ affinity, the us/sy split, the RX-softirq coupling, and io_uring as the fix."}},{key:"containers",tab:"Containers",title:"Containers — cgroups & namespaces",accent:"mem",one_liner:"The wrapper around every process: namespaces decide what it can SEE, cgroups cap what it can USE.",mechanism:'A container is just a process with namespaces on (a private view of one kernel resource each — pid/net/mnt/uts/ipc/user/cgroup/time, the kernel still shared) and a cgroup v2 node capping its resources. cpu.max is a hard quota/period ("10000 100000" = 0.10 CPU): a burst needing more than its slice is parked until the next CFS period even while host cores idle — runnable-but-not-running, invisible to average %us, surfacing only as nr_throttled/throttled_usec in cpu.stat (never as %steal — that’s hypervisor). memory.max is a hard ceiling: hit it and the kernel reclaims clean cache, then the cgroup OOM killer fires (exit 137). Here swap=0, so anon is unreclaimable — the OOM killer is the floor, not graceful swapping.',readouts:[{label:"cgroup shown",field:"cg.comm",unit:"",watch:"the box’s ONE most-constrained cgroup (ranked by quota+throttle+mem) — often an idle quota’d daemon, NOT your app and NOT an aggregate"},{label:"throttle %",field:"cg.thr_pct",unit:"%",watch:"> 0 sustained for a latency service ⇒ mis-sized cpu.max, not a CPU shortage"},{label:"throttled periods",field:"cg.nr_throttled",unit:"count",watch:"rising while node has idle cores ⇒ quota stall; raise cpu.max or use cpu.weight"},{label:"cpu.max quota",field:"cg.cpu_max",unit:"CPU",watch:"tiny (e.g. 0.10) on bursty/latency work ⇒ guaranteed throttling"},{label:"mem vs max",field:"cg.mem_pct",unit:"%",watch:"> 90 ⇒ reclaim then OOM-kill imminent; swap=0 so anon can’t page out"},{label:"mem.current",field:"cg.mem_cur_mb",unit:"MB",watch:"climbing monotonically toward the cap ⇒ a leak heading to exit 137"},{label:"PID-ns isolated",field:"cg.nsiso",unit:"",watch:"no ⇒ shares the host PID namespace (hostPID) — this probe is PID-ns only, not net/mnt/uts"}],costs:[["context switch","~1.7 µs"],["syscall trap","~83 ns"],["minor page fault","~703 ns"],["CFS period","100 ms default"],["real cgroup","cpu.max 0.10 CPU, throttled"],["swap","0 → OOM is the floor"]],fails:[{sig:"CPU “fine” but throttled",bad:"throttle % > 0 (or periods rising) while host cores idle and mem normal ⇒ cpu.max quota, not steal (%st=0); raise limit or use cpu.weight"},{sig:"OOMKilled (exit 137)",bad:"mem % hits ~100 then memory.events oom_kill++; current pinned at max. swap=0 means no paging reprieve"},{sig:"Throttle-induced tail latency",bad:"nr_throttled accrues each period (thr % > ~5%) on a latency service even at low average CPU"},{sig:"Wrong isolation boundary",bad:"a shared/missing namespace (sidecar can’t resolve DNS, or hostPID on an app pod); lsns shows owner-less entries"}],ref:{ids:["cgroups","namespaces","oci-runtime"],note:"the quota/throttle + OOM model (cpu.max vs cpu.weight), the 8 CLONE_NEW* views + the leak, and how runc/crun assemble both for a pod."}}];export{e as S};
